<%- include('../partials/header', { pageTitle: 'One City Events', styles: '../../css/styles.css' ,event: '../../css/event.css'}) %>



<div class="rent-venue-application d-flex justify-content-center align-items-center">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6 col-md-8 col-sm-12">
                <!-- Heading -->
                <div class="heading-text text-center mb-4">
                    <h4>Admin Sign In</h4>
                </div>

                <!-- Error Message -->
                <% if (sessionData.errorMessage) { %>
                    <div class="alert alert-danger text-center mb-4">
                        <%= sessionData.errorMessage %>
                    </div>
                <% } %>

                <!-- Contact Form -->
                <div class="contact-form bg-white p-4 rounded shadow-sm">
                    <form id="contact" action="/auth/admin/signin" method="post">
                        <div class="row g-3">
                            <!-- Email -->
                            <div class="col-12">
                                <div class="form-floating">
                                    <input name="email" type="email" id="email" class="form-control" placeholder="Your Email*" required>
                                    <label for="email">Your Email*</label>
                                </div>
                            </div>

                            <!-- Password -->
                            <div class="col-12">
                                <div class="form-floating">
                                    <input name="password" type="password" id="password" class="form-control" placeholder="Password*" required>
                                    <label for="password">Password*</label>
                                </div>
                            </div>

                            <!-- Sign In Button -->
                            <div class="col-12">
                                <button type="submit" id="form-submit" class="btn btn-primary w-100">Sign In</button>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


<%- include('../partials/footer') %>